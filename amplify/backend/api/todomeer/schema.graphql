type Todo @model @auth(rules: [{ allow: public }]){
  id: ID!
  name: String!
  image:String!
  userId: String!
}
 

type UserTodoCount @model @auth(rules: [{ allow: public }]){
id: ID!
userID: String!
count: Int!
}
